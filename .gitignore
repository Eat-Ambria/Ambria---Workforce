# dependencies
node_modules

# build output
dist
dist-ssr
dev-dist

# database scripts — kept local, run by hand in the Supabase SQL Editor
supabase/db/migrations/

# local env (SECRETS — never commit)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
