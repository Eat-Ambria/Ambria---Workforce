# dependencies
node_modules

# build output
dist
dist-ssr
dev-dist

# database scripts — LOCAL ONLY, like .env. Run by hand in the Supabase SQL
# Editor. Keep a backup: they are not in the repo, so a fresh clone has no way
# to rebuild the database.
supabase/db/migrations/

# local env (SECRETS — never commit)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
